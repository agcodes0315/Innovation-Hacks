import "dotenv/config";import fs from "node:fs";import path from "node:path";const f=path.resolve(process.cwd(),process.env.DATABASE_FILE||"./data/devflow.db");for(const s of ["","-shm","-wal"]){if(fs.existsSync(f+s))fs.unlinkSync(f+s)}console.log("Database removed. Restart to recreate.");
